# Test/dev toolchain for garden_irrigation (Milestone 1).
# Kept in sync with the `test`/`dev` optional-dependency groups in pyproject.toml
# so both `pip install -r requirements_test.txt` and `uv sync --extra test --extra dev`
# resolve to the same toolchain.
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-homeassistant-custom-component
syrupy>=4.0
ruff>=0.8
mypy>=1.13
